{"previewPort": 3000, "healthPath": "/", "startCommand": "docker compose -f docker-compose.base44.yml up -d", "secrets": [{"name": "TRACERFY_API_KEY", "description": "Tracerfy MCP connector token for skip tracing property owners (phone/email lookup). Generate it in your Tracerfy profile under 'Connect via MCP' — paste just the token value.", "requiredAtBoot": false}]}