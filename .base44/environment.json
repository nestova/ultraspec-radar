{"previewPort": 3000, "healthPath": "/", "startCommand": "docker compose -f docker-compose.base44.yml up -d", "secrets": [{"name": "TRACERFY_API_KEY", "description": "Tracerfy MCP connector token. Generate it in your Tracerfy profile under 'Connect via MCP' — it appears in the URL https://mcp.tracerfy.com/u/<token>/mcp. Paste just the token value.", "requiredAtBoot": false}]}